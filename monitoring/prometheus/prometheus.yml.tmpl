global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: adaptive_ads_router
    metrics_path: /ops/metrics/prometheus
    params:
      token: ["__AAR_ADMIN_TOKEN__"]
    static_configs:
      - targets: ["router:8024"]
